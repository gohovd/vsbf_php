function showMessage(type, message) {
    var msg_div = document.createElement("div");
        msg_div.id = "msg_div";
        msg_div.classList.add("label");

    switch (type) {
        case "warning":

        break;
        case "danger":

        break;
        case "info":
            
        break;
    }
}